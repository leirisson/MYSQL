/* Escreva uma consulta SQL para selecionar todos os
produtos vendidos que tenham um preço superior a R$500 e inferior a R$1000. */

SELECT * FROM alugueis
WHERE preco_total >= 500 AND preco_total <= 1000;